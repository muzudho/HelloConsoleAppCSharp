# 第１章第２節：ファイルからメッセージを読込もう　＞　ウォーミングアップ

表示する文章をソースコードに埋め込むのではなく、テキスト・ファイルに保存しておいて、それを読込むようにする方法を説明します。  
テキストファイルの形式は色々有りますが、このアプリケーションでは JSON形式をメインに使用します。  

また、このアプリケーションでは多国語への翻訳のことは考慮しません。  


## フォルダーとファイルの構成

以下のようにフォルダーとファイルを用意してください。  

```plaintext
📁 HelloConsoleAppCSharp
+-- 📁 Assets
|   +-- 📄 MessagesWarmup.json                   # ファイルを新規作成
+-- 📁 Features                                  # フォルダーを新規作成
|   +-- 📁 Messages                              # フォルダーを新規作成
|       +-- 📄 MuzReadMessagesWarmupCommand.cs   # ファイルを新規作成
+-- 📄 ProgramCommands.cs
```


## JSON形式ファイルの作成

📄 `HelloConsoleAppCSharp/Assets/MessagesWarmup.json`:  

```json
{
  "セメント": "水と混ぜると固まる。\n石灰石などを細かく粉砕したもの。建築の材料。",
  "ノロ": "セメントを水だけに溶かして練ったもの。\nきめが細かいのでひび割れの補修に使える。\n建築の材料。",
  "モルタル": "セメントに砂を混ぜて水に溶かして練ったもの。\nブロックの目地に使ったり、仕上げに使われる。\n建築の材料。",
  "コンクリート":  "セメントに砂や砂利を混ぜて水に溶かして練ったもの。\n圧縮する力には強いが、引っ張る力には弱い。"
}
```

👆 Visual Studio 2026 でこのファイルを右クリックしてプロパティを開き、［ビルドアクション］を［コンテンツ］に、  
［出力ディレクトリにコピー］を［常にコピーする］に設定してください。  


## コマンドの作成

📄 `HelloConsoleAppCSharp/Features/Messages/MuzReadMessagesWarmupCommand.cs`:  

```csharp
namespace HelloConsoleAppCSharp.Features.Messages;

using HelloConsoleAppCSharp.Infrastructure.REPL;
using System.Text.Json;

internal static class MuzReadMessagesWarmupCommand
{
    internal static async Task<MuzRequestType> ExecuteAsync(
        IServiceProvider services)
    {
        try
        {
            // TODO: Messages.json ファイルを読込みます。
            string filePath = "Assets/MessagesWarmup.json";  // ← あなたのJSONファイルのパスに変更

            // ファイル全体を文字列として読み込む
            string jsonString = File.ReadAllText(filePath);

            // Dictionary<string, string> にデシリアライズ
            Dictionary<string, string>? items = JsonSerializer.Deserialize<Dictionary<string, string>>(jsonString);

            if (items != null)
            {
                Console.WriteLine("読み込み成功！アイテム一覧：\n");

                foreach (var item in items)
                {
                    Console.WriteLine($"【{item.Key}】");
                    Console.WriteLine(item.Value);
                    Console.WriteLine("-------------------");
                }

                var target = "セメント";
                Console.WriteLine($"［{target}］の説明をもう１回見てみるぜ（＾～＾）");

                // 特定のアイテムを取り出す例
                if (items.TryGetValue(target, out string? desc))
                {
                    Console.WriteLine($"{target}の説明: {desc}");
                }
            }

            return MuzRequestType.None;
        }
        catch (Exception ex)
        {
            // TODO: ログファイル出力したい。
            Console.WriteLine($"エラー: {ex.Message}");
            throw;
        }
    }
}
```


## コマンドの登録

📄 `HelloConsoleAppCSharp/ProgramCommands.cs`:  

```csharp
～前後略～

/// <summary>
/// コマンド実行部
/// </summary>
internal static class ProgramCommands
{
    internal static async Task<MuzRequestType> ExecuteAsync(
        string command,
        IServiceProvider services)
    {


        ～中略～


        switch (commandName)
        {


            // ～中略～
            
            
            // ----------------------------------------
            // 第１章第２節：ファイルからメッセージを読込もう
            // ----------------------------------------


            // ［メッセージ］ファイルの読込の動作確認
            case "read-messages-warmup": return await MuzReadMessagesWarmupCommand.ExecuteAsync(services);
            
            
            // ～中略～


            default:
                Console.WriteLine($"知らないコマンドだぜ: {command}");
                return MuzRequestType.None;
        }
    }
}
```

これで、このコンソール・アプリケーションを起動し、 `read-messages-warmup` と入力すると、［メッセージ］ファイルの読込の動作確認ができます。  

