# サービス追加の練習

この記事では、サービスを追加する練習をします。  


## フォルダー構成

```plaintext
📁 ソリューション 'HelloConsoleAppCSharp'
+-- 📁 HelloConsoleAppCSharp
    +-- 📄 ApplicationService.cs         # （後述）ファイルを新規作成
    +-- 📄 Program.cs                    # （後述）既存のファイルを編集
```


## サービスの新規作成

以下のファイルを作成してください。  

📄 `HelloConsoleAppCSharp/ApplicationService.cs`:  

```csharp
namespace HelloConsoleAppCSharp;

using System;

/// <summary>
///     <pre>
/// ［作りたいもの］と［作るのを便利にするもの］に分けたとき、  
/// 前者を［ドメインモデル］、後者を［インフラストラクチャー］と呼ぶことがあるぜ（＾～＾）！
/// 
/// それを踏まえて、［ドメインモデル］の方で使い、［インフラストラクチャー］の方では使わないデータを、詰め込むのがこの［アプリケーション・サービス］クラスだぜ（＾～＾）！
///     </pre>
/// </summary>
internal class ApplicationService
{


    // ========================================
    // 構成
    // ========================================


    #region ［アプリケーションの起動日時］

    /// <summary>
    /// アプリケーションの起動日時を格納しておくぜ（＾～＾）！
    /// </summary>
    internal DateTime LaunchDateTime { get; set; }

    #endregion


}
```


## プログラムへサービスを登録

以下のコードを追加してください。  

📄 `HelloConsoleAppCSharp/Program.cs`:  

```csharp


～前略～


    await MuzHostHelper.RunAsync(
        commandLineArgs: args,
        beforeHostBuild: async (builder, executeBeforeBuild) =>
        {


            ～中略～


            //
            // ［ロギング］サービスの登録
            //
            await MuzLogging.SetupBeforeHostBuildAsync(
                builder: builder,
                onBootstrapLoggingEnabled: async (bootstrapLogger) =>
                {
                    // ここから `bootstrapLogger` を使った［ロギング］できる（＾～＾）！
                    bootstrapLogger.LogInformation("ホストビルド前だが、ブートストラップ・ログは出せるぜ（＾～＾）！");


                    // 📍 NOTE:
                    //
                    //      （あとで）ここへサービスを追加していくぜ（＾～＾）
                    //
                    await executeBeforeBuild(builder.Services);


                });


        },
        executeBeforeBuild: async (services) =>
        {


            // 📍 NOTE:
            //
            //      ここで、あとで［サービスの登録］とかやるぜ（＾▽＾）！
            //


            //
            // ［アプリケーション］サービスの登録
            //
            services.AddScoped<ApplicationService>();   // ⭐ これを追加（＾～＾）！


        },
        afterHostBuild: async (builder, services, executeAfterHostBuild) =>
        {
            ～中略～
        },
        executeAfterHostBuild: async (services) =>
        {
            ～中略～
        });


～後略～


```

今回はここまでです。  
